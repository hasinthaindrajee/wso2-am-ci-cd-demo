environments:
  - name: test
    endpoints:
      production:
        url: 'http://ws.cdyne.com/phoneverify/phoneverify.asmx'
        config:
          retryTimeOut: $RETRY
    gatewayEnvironments:
      - newgwtest
  - name: prod
    endpoints:
      production:
        url: 'http://ws.cdyne.com/phoneverify/phoneverify.asmx'
        config:
          retryTimeOut: $RETRY
